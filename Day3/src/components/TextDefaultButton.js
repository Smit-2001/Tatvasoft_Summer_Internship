import React from 'react';
import Button from '@material-ui/core/Button';

const TextDefaultButton = () => {
  return (
    <Button variant="text" color="default">
      Click Me
    </Button>
  );
};

export default TextDefaultButton;
